Write a detailed and welcoming readme file for the new `cgp-example-profile-picture` demo repository that is created to demonstrate the capabilities of `#[cgp_fn]` and `#[cgp_impl]`.

The documentation should contain a tutorial that starts from the basic form in `monolithic_fn`, and evolve toward `plain_fn`, `monolithic_context`, `monolithic_trait`, and `cgp_fn`. The `cgp_fn_generics` and `cgp_impl` are sneak preview of more advanced capabilities in CGP. The `cgp_fn_desugared` shows the desugared version of `cgp_fn`.

The tutorial should be organized into detailed chapters, with each chapter covering one step of the evolution. Each chapter should contain further subsections that describe the issues with the previous approach, the new approach, together with code snippets of all functions from the particular source file.

Provide sufficient explanation and motivation of each approach, and the trade offs made.

The target audience are beginner Rust programmers who learn about CGP the first time, and have not read any other documentation about CGP.

Use full sentences and avoid point forms and tables.